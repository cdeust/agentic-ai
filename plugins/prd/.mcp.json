{
  "_note": "Wired to the unified TS MCP server at packages/mcp-servers/prd/dist/index.js. CLAUDE_PLUGIN_ROOT resolves to plugins/prd/ when installed via the agentic-ai marketplace; the relative path walks up to repo root then into the built MCP server. See cf85cfc in cdeust/automatised-pipeline (2026-04-25) for the bash -c expansion pattern.",
  "mcpServers": {
    "prd-gen": {
      "command": "bash",
      "args": [
        "-c",
        "exec node \"${CLAUDE_PLUGIN_ROOT}/../../packages/mcp-servers/prd/dist/index.js\""
      ],
      "env": {
        "PRD_GEN_EVIDENCE_DB": "${HOME}/.agentic-ai/prd-gen/evidence.db"
      }
    }
  }
}
