{
  "_note": "Wired to the unified TS MCP server at packages/mcp-servers/memory/dist/index.js. CLAUDE_PLUGIN_ROOT resolves to plugins/memory/ when installed via the agentic-ai marketplace; the relative '../../packages/mcp-servers/memory/dist/index.js' walks up to repo root then into the built MCP server. The bash -c wrapper exists because Claude Code only substitutes the plain ${CLAUDE_PLUGIN_ROOT} pattern; using bash -c lets the shell handle expansion at invocation time. See cf85cfc in cdeust/automatised-pipeline (2026-04-25) for root-cause.",
  "mcpServers": {
    "cortex": {
      "command": "bash",
      "args": [
        "-c",
        "exec node \"${CLAUDE_PLUGIN_ROOT}/../../packages/mcp-servers/memory/dist/index.js\""
      ],
      "env": {
        "DATABASE_URL": "",
        "CORTEX_MEMORY_AP_ENABLED": "1"
      }
    }
  }
}
