{
  "_note": "Wired to the unified TS MCP server at packages/mcp-servers/reasoning/dist/index.js. CLAUDE_PLUGIN_ROOT resolves to plugins/reasoning/ when installed via the agentic-ai marketplace; the relative path walks up to repo root then into the built MCP server. See cf85cfc in cdeust/automatised-pipeline (2026-04-25) for the bash -c expansion pattern.",
  "mcpServers": {
    "reasoning": {
      "command": "bash",
      "args": [
        "-c",
        "exec node \"${CLAUDE_PLUGIN_ROOT}/../../packages/mcp-servers/reasoning/dist/index.js\""
      ]
    }
  }
}
