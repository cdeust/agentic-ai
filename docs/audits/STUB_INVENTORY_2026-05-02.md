# Stub Inventory — 2026-05-02

Generated by Borges structural audit. Search vectors 1–12 run exhaustively
against all `packages/*/src`, `packages/mcp-servers/*/src`, and
`packages/prd-pipeline/packages/*/src`. Special focus areas A–H verified by
direct file inspection.

---

## §1 Executive Summary

| Classification | Count |
|---|---|
| CRIT — fakes implementation (returns silently, behaviour absent) | 9 |
| HIGH — runtime throw (PortPendingError / explicit Error) | 11 |
| MED — comment-only deferral (TODO/port-pending, code path live but incomplete) | 12 |
| MED — test-todo (it.todo, live-gated tests) | 11 |
| LOW — comment marker with no behaviour gap | 5 |
| **Total unique sites** | **48** |

Zero `CORTEX_ABLATE_*` env vars found in any `src/` tree (the 23 ablation vars
live in the Python Cortex server, not yet ported to TypeScript).

---

## §2 Per-Package Marker Tally

| Package | CRIT | HIGH | MED | MED-test-todo | LOW |
|---|---|---|---|---|---|
| packages/memory | 7 | 10 | 10 | 8 | 3 |
| packages/orchestrator | 0 | 0 | 1 | 0 | 2 |
| packages/mcp-servers/codebase | 0 | 0 | 1 | 0 | 0 |
| packages/mcp-servers/reasoning | 0 | 0 | 1 | 0 | 0 |
| packages/mcp-servers/prd | 0 | 0 | 1 | 0 | 0 |
| packages/core | 1 | 0 | 0 | 0 | 0 |
| packages/codebase | 0 | 1 | 1 | 3 | 0 |
| packages/prd-pipeline | 0 | 0 | 0 | 0 | 0 |

---

## §3 Exhaustive Marker Catalogue

Format: `file:line | classification | pretends to do | actually does`

---

### CRIT — Fakes Implementation

**CRIT-001**
`packages/memory/src/recall/` — `spreading-activation.ts` and `dendritic-clusters.ts`
Source files **do not exist**. Only compiled `dist/recall/spreading-activation.js` and
`dist/recall/dendritic-clusters.js` remain (orphaned from a previous build).
`recall-handler.ts` and `multi-signal-fusion.ts` in `src/` reference these modules in
comments and signal fields but do not import them. Signals `hopfield`, `hdc`, `sr`, `sa`
are hardwired to `[]` in `recall-handler.ts:243-246`.
Classification: **CRIT** — spreading activation and dendritic-cluster signals advertised
in `RecallResponse`, silently absent from every query response.

**CRIT-002**
`packages/memory/src/recall/recall-handler.ts:236-246`
```
// 4. Assemble signals (no Hopfield/HDC/SR/SA — those are port-pending)
hopfield: [],
hdc: [],
sr: [],
sa: [],
```
Pretends: multi-signal fusion pipeline with Hopfield, HDC, SR, SA.
Actually: these four signal channels always contribute zero to RRF fusion.
Classification: **CRIT** — silent zero, not a thrown error, consumer cannot detect.

**CRIT-003**
`packages/memory/src/recall/types.ts:85-89`
```
// These rely on PG stored procs — they are port-pending at the boundary
hopfield: z.array(...).default([]),
hdc: z.array(...).default([]),
sr: z.array(...).default([]),
sa: z.array(...).default([]),
```
Pretends: schema slots are ready to receive PG-computed signals.
Actually: schema defaults silently to `[]`; no PG stored-proc adapter exists.
Classification: **CRIT** — schema lies about what is implemented.

**CRIT-004**
`packages/memory/src/wiki/handlers/wiki-stubs.ts:203-224`
`wikiSynthesizeHandler` — the function accepts `llmClient` and `args` but immediately
`void`s both and returns hardcoded zeros:
```
drafts_created: 0, drafts_updated: 0, memories_processed: 0, by_kind: {}, errors: [],
note: "wiki-synthesize: LLM client gate resolved ... pg_store_wiki adapter still required"
```
Pretends: wiki synthesis is half-working (LLM client gate is cleared).
Actually: returns zero/empty for every field regardless of input. Caller receives a
success-shaped response with zero work done.
Classification: **CRIT** — success return masking a complete no-op.

**CRIT-005**
`packages/memory/src/wiki/handlers/wiki-stubs.ts:258-275`
`wikiPipelineHandler` — same pattern: args and llmClient voided, `stages_run: []`.
Pretends: pipeline orchestration with LLM client available.
Actually: always returns `stages_run: []`; no stage runs.
Classification: **CRIT** — success-shaped no-op.

**CRIT-006**
`packages/memory/src/workflow-graph/sources/source-ast.ts:307-310`
```
loadSymbols(filePaths): Record<string, unknown>[] {
  // port-pending: sync façade over async AP calls. Returns [] immediately;
  void this.loadSymbolsAsync(filePaths);
  return [];
}
```
Pretends: synchronous symbol loading.
Actually: fires async work and immediately returns `[]`. Callers expecting symbols get
nothing. The async work result is silently discarded.
Classification: **CRIT** — the return value is always wrong when called synchronously.

**CRIT-007**
`packages/memory/src/workflow-graph/sources/source-ast.ts:342-345`
`loadAstEdges` — identical pattern to CRIT-006: fires async, returns `[]`.
Pretends: synchronous edge loading.
Actually: always `[]`.
Classification: **CRIT**.

**CRIT-008**
`packages/memory/src/hooks/post-tool-capture.ts:230-241`
```
log(`would store ${toolName} → db=${databaseUrl}, tags=...`);
// TODO: wire to packages/memory/src/remember/index when merged (merge order #2).
void directory;
```
Pretends: captures tool outputs and stores them to the remember handler.
Actually: logs a "would store" message and returns. No storage occurs.
Classification: **CRIT** — hook is live in the Claude Code session lifecycle; every
tool use silently discards its capture.

**CRIT-009**
`packages/memory/src/hooks/compaction-checkpoint.ts:46-50`
```
log("cascade advancement: stub (wire to consolidation when merged)");
// TODO: wire to checkpoint handler (merge order #2) and cascade handler (merge order #4).
```
Pretends: saves a memory checkpoint and advances the consolidation cascade on
compaction/session-end events.
Actually: logs the intent and returns. No checkpoint is written.
Classification: **CRIT** — consolidation cascade never fires; memory decay schedule
is not maintained.

---

### HIGH — Runtime Throw (PortPendingError / explicit Error)

**HIGH-001**
`packages/memory/src/wiki/handlers/wiki-stubs.ts:281-287`
`wikiEmergeHandler` — always throws `PortPendingError("wiki-emerge", ..., "pg_store_wiki (concept emergence DB pass)")`.
Requires: `pg_store_wiki` adapter.

**HIGH-002**
`packages/memory/src/wiki/handlers/wiki-stubs.ts:293-299`
`wikiExtractHandler` — always throws `PortPendingError("wiki-extract", ..., "pg_store_wiki (claim extraction + persistence)")`.

**HIGH-003**
`packages/memory/src/wiki/handlers/wiki-stubs.ts:305-311`
`wikiCurateHandler` — always throws `PortPendingError("wiki-curate", ..., "pg_store_wiki (draft approval/rejection persistence)")`.

**HIGH-004**
`packages/memory/src/wiki/handlers/wiki-stubs.ts:317-323`
`wikiConsolidateHandler` — always throws `PortPendingError("wiki-consolidate", ..., "pg_store_wiki (thermodynamic heat decay + staleness flags)")`.

**HIGH-005**
`packages/memory/src/wiki/handlers/wiki-stubs.ts:329-335`
`wikiResolveHandler` — always throws `PortPendingError("wiki-resolve", ..., "pg_store_wiki (claim supersedes + conflict persistence)")`.

**HIGH-006**
`packages/memory/src/wiki/handlers/wiki-stubs.ts:341-347`
`wikiSeedCodebaseHandler` — always throws `PortPendingError("wiki-seed-codebase", ..., "cortex-codebase-analysis (#5) + LLM client")`.

**HIGH-007**
`packages/memory/src/wiki/handlers/wiki-stubs.ts:353-359`
`wikiExportHandler` — always throws `PortPendingError("wiki-export", ..., "Pandoc subprocess integration")`.

**HIGH-008**
`packages/memory/src/wiki/handlers/wiki-stubs.ts:365-371`
`wikiCompileHandler` — always throws `PortPendingError("wiki-compile", ..., "pg_store_wiki + wiki_store")`.

**HIGH-009**
`packages/memory/src/wiki/handlers/wiki-stubs.ts:377-383`
`wikiMigrateHandler` — always throws `PortPendingError("wiki-migrate", ..., "pg_store_wiki + schema versioning")`.

**HIGH-010**
`packages/memory/src/wiki/handlers/wiki-stubs.ts:389-395`
`wikiApiHandler` — always throws `PortPendingError("wiki-api", ..., "HTTP server + pg_store_wiki")`.

**HIGH-011**
`packages/codebase/src/adapters/rust-pipeline-adapter.ts:8` (header comment)
Tools 18–23 (6 tools) are declared deferred to Phase 6. The `CodebasePort` interface
in `packages/core/src/ports/codebase.ts:6` acknowledges this deferral. No implementation
exists; calling these port methods throws a validation error at runtime because the
JSON-RPC method does not exist in the binary.
Requires: Phase 6 / binary update.

---

### MED — Comment-Only Deferral

**MED-001**
`packages/memory/src/recall/co-activation.ts:13,27`
```
// TODO(port-pending): Full entity extraction requires porting
// knowledge_graph.extract_entities
```
`extractEntitiesSimple` uses a capitalized-token regex instead of the full Python NER.
The function is live and runs; it produces approximate entity sets. Gap: precision of
co-activation edges is lower than the Python implementation.
Requires: `port/cortex-graph-navigation` worktree.

**MED-002**
`packages/memory/src/recall/rules.ts:10`
```
// TODO(port-pending): Full rule evaluation (evaluate_rules, MemoryRule)
// requires porting mcp_server/core/memory_rules.py
```
`applyRules` is implemented and runs (boost/penalize/pin/suppress). The deferral is
for the full persistence layer (rule CRUD, DB storage of MemoryRule objects).
Requires: `port/cortex-automation` worktree.

**MED-003**
`packages/memory/src/recall/recall-hierarchical-handler.ts:23,68,107`
Three `TODO(port-pending)` markers for `fractal.buildHierarchy`,
`fractal.buildHierarchy` (real), and `fractal.scoreAgainstHierarchy`.
The handler has a live greedy-clustering approximation; it degrades gracefully to flat
recall below 3 embeddings. The real fractal module differs in L2 super-clusters and
centroid update algorithm.
Requires: `port/cortex-graph-navigation` worktree.

**MED-004**
`packages/memory/src/remember/handlers/remember.ts:190`
```
// port-pending: full knowledge_graph.extract_entities is not yet ported.
```
`extractEntityNamesFromContent` uses a stopword-filtered capitalized-token heuristic.
Same gap as MED-001: entity extraction is approximate.
Requires: `port/cortex-graph-navigation` worktree (same blocker as MED-001).

**MED-005**
`packages/memory/src/hooks/session-lifecycle.ts:218`
```
log(`consolidation stub: mode=${mode} ...`);
// TODO: wire to packages/memory/src/consolidation/ (merge order #4).
```
The mode-classification logic (light/standard/full) is implemented; the downstream
consolidation package call is missing. The hook fires correctly; consolidation is absent.
Requires: `packages/memory/src/consolidation/` package (merge order #4).

**MED-006**
`packages/memory/src/hooks/compaction-checkpoint.ts:50`
Identical to MED-005 for the checkpoint pathway. The hook fires; the downstream
remember/consolidation call is stubbed with a log statement.
Requires: merge order #2 (remember) + merge order #4 (cascade).

**MED-007**
`packages/memory/src/workflow-graph/builder.ts:176`
```
// TODO: wire into the shared domain-mapping package when available.
```
`assignDomain` is structurally correct for the common case; only the domain-mapping
package integration (Python equivalent) is missing. Non-blocking for most callers.
Requires: `port/core-types` or `port/domain-mapping` worktree.

**MED-008**
`packages/memory/src/recall/port.ts:175`
```
// TODO(Group A): flip this import to re-export from @agentic/core once the
// composition root ships an adapter from Float32Array to number[].
```
The stub `EmbeddingEngine` interface in `recall/port.ts` (returns `number[]`) is separate
from the canonical `@agentic/core` EmbeddingEngine (returns `Float32Array`). The
`toRecallEmbeddingEngine` adapter in `transformers-embedding-engine.ts` bridges these but
is **not wired** at the MCP composition root. No composition root for the memory MCP
server yet wires this adapter.
Requires: composition root wiring (Group A, Phase 7).

**MED-009**
`packages/memory/src/infrastructure/transformers-embedding-engine.ts:209`
Note in the file points to `recall/port.ts` TODO(Group A). The adapter
(`toRecallEmbeddingEngine`) is fully implemented but is not invoked from any composition
root. The embedding engine is wired for the `remember` path (standalone) but not for the
`recall` path's multi-signal fusion.

**MED-010**
`packages/mcp-servers/codebase/src/index.ts:4`
`STATUS: port-pending` — entire composition root is a `PORT_STATUS = "pending"` export.
No tools are registered. The memory MCP server works; codebase MCP server does nothing.
Requires: Phase 6 codebase-server worktree.

**MED-011**
`packages/mcp-servers/reasoning/src/index.ts:4`
Same: `PORT_STATUS = "pending"`. No reasoning tools registered.
Requires: dedicated worktree (port/reasoning-server or Phase 6).

**MED-012**
`packages/mcp-servers/prd/src/index.ts:4`
Same: `PORT_STATUS = "pending"`. No PRD tools registered.
Requires: `@agentic/prd-pipeline` wiring (Phase 6 / post-migration).

---

### MED — Test-Todo (live-gated or infrastructure-gated tests)

**TEST-001–005**
`packages/memory/__tests__/workflow-graph/source-ast.test.ts:182-204`
Five `it.todo(...)` blocks gated on "live AP graph required":
- `loadSymbolsAsync` load-all mode uncapped symbol list
- `loadSymbolsAsync` Import node qualified-name surrogate
- `loadAstEdgesAsync` Defines_File_Import edges
- `loadAstEdgesAsync` Uses_Method_Struct edges
- `loadAstEdgesAsync` Cartesian call labels
All require a running automatised-pipeline binary + populated graph.

**TEST-006–007**
`packages/memory/__tests__/remember/embedding-engine.test.ts:389-397`
Two `it.todo(...)` for live model tests:
- "loads Xenova/all-MiniLM-L6-v2 and returns 384-dim vectors [live: set AGENTIC_EMBED_LIVE=1]"
- "embed() output is L2-normalised (live model, AGENTIC_EMBED_LIVE=1)"
Gate: `AGENTIC_EMBED_LIVE=1`. Not a stub — deliberately env-gated to avoid 90 MB CI download.

**TEST-008**
`packages/memory/__tests__/narrative/llm-client-narrative.test.ts:250`
`it.todo(...)` for one narrative test scenario. Single item; no label recorded.

**TEST-009–011**
`packages/codebase/__tests__/parity/index_codebase.parity.test.ts:117-124,241`
Three `it.todo(...)` items:
- "requires PR #18 merged + cargo build (AI_ARCH_BIN not set)"
- fixture repo not found (conditional)
- "requires PR #18 merged + cargo build" (line 241)
Gate: binary must be present via `AI_ARCH_BIN` or `@agentic/codebase-rust` workspace.

---

### LOW — Comment Marker, No Behaviour Gap

**LOW-001**
`packages/core/src/index.ts:32-34`
```
// Legacy placeholder — kept so existing consumers of `__PLACEHOLDER__` don't hard-fail
export const __PLACEHOLDER__ = true;
```
This is a shim to prevent import errors from old consumers. Not a fake implementation;
it is an intentional backward-compat export with no caller consequence.

**LOW-002**
`packages/orchestrator/src/index.ts:10-12`
Port-pending comments in the architecture diagram comment block (lines 10-12).
The orchestrator itself is a live skeleton; it only launches live servers.
The three pending server entries in `defaultServerConfigs()` have `status: "pending"`
and are filtered out at runtime — no dead code path in production logic.

**LOW-003**
`packages/memory/src/automation/trigger-matcher.ts:15,40-41`
The regex patterns for `TODO` and `FIXME` in user content are search targets for
prospective-memory triggering, not stub markers in the implementation.

**LOW-004**
`packages/codebase/src/index.ts:10`
Inline comment explaining degraded mode: "degrade gracefully should catch and return
a stub; they own that decision." Describes caller contract; no implementation gap here.

**LOW-005**
`packages/codebase-rust/src/main.rs:539`
`// spec §9.3 Q1 — .md input is deferred in v1.` — Rust-side deferred feature note,
not a TS stub. Out of scope for TS agent.

---

## §4 Recommended Dispatch Plan

### Tier A — Single engineer-agent, single PR (self-contained)

| ID | Action | Blocker removed |
|---|---|---|
| CRIT-006, CRIT-007 | Remove `loadSymbols` / `loadAstEdges` sync façades or convert to `async` and update callers | AP pool plumbing not needed; just fix the return contract |
| CRIT-001 | Restore `spreading-activation.ts` and `dendritic-clusters.ts` source files to `packages/memory/src/recall/` from dist artifacts (reverse the source-map) and wire imports in `recall-handler.ts` | None — sources exist as dist; reconstruct or check git history |
| CRIT-002, CRIT-003 | Wire spreading-activation and dendritic-cluster signals into `recall-handler.ts` signal assembly | Depends on CRIT-001 |
| MED-001, MED-004 | Promote `extractEntitiesSimple` to a better regex or import from `graph/heat-propagation.ts` which already has `resolveEntityNames`; reduces gap without full NER port | None |
| LOW-001 | Remove `__PLACEHOLDER__` export once all consumers are confirmed migrated | Requires `port/core-types` audit |

### Tier B — Single engineer-agent, cross-cutting but scoped (one worktree per item)

| ID | Action | Blocker removed |
|---|---|---|
| CRIT-008 | Wire `post-tool-capture.ts` to the `remember` handler (`packages/memory/src/remember/`) — the package exists; the call is just missing | merge order #2 already complete |
| CRIT-009, MED-005, MED-006 | Wire session-lifecycle and compaction-checkpoint hooks to `packages/memory/src/consolidation/` (or confirm the package does not exist yet) | merge order #4 |
| MED-008, MED-009 | Wire `toRecallEmbeddingEngine` adapter into the memory MCP composition root so recall multi-signal fusion gets real embeddings | Requires MCP server composition root |
| CRIT-004, CRIT-005 | Implement `wikiSynthesizeHandler` synthesis loop and `wikiPipelineHandler` pipeline; the pg_store_wiki blocker is explicit — open a Phase 7 Group D tracking issue | Phase 7 Group D |

### Tier C — Multi-engineer or phased (cross-cutting, external dependency)

| ID | Action | Blocker removed |
|---|---|---|
| HIGH-001–010 | 10 wiki handlers blocked on `pg_store_wiki` adapter; one worktree creates the PG wiki store adapter | Phase 7 Group D |
| HIGH-011 | CodebasePort tools 18–23 require Phase 6 binary update and adapter implementation | Phase 6 binary |
| MED-010–012 | Three MCP server composition roots (codebase, reasoning, prd) each require their own wiring worktree | Phase 6 |
| MED-003 | Real fractal clustering for hierarchical recall | `port/cortex-graph-navigation` |
| TEST-001–005 | Live AP graph tests require a seeded automatised-pipeline instance | CI fixture |
| TEST-009–011 | Codebase parity tests require PR #18 + cargo build | PR #18 |

---

## §5 The Ablation Env-Var Contract (CORTEX_ABLATE_*)

**Findings: zero `CORTEX_ABLATE_*` env vars exist in any TypeScript `src/` tree.**

The 23 ablation env vars referenced in PR #23 are Python-side only
(`mcp_server/` in the Cortex Python server). They have not been ported to TypeScript.

Two ablation references appear in the `dist/recall/` compiled files
(`CORTEX_ABLATE_SPREADING_ACTIVATION` in `spreading-activation.js`,
`CORTEX_ABLATE_DENDRITIC_CLUSTERS` in `dendritic-clusters.js`) — but these are compiled
artifacts whose source files are missing from `src/` (see CRIT-001).

**Contract status:**

| Ablation var | TS src file | Implemented? | Classification |
|---|---|---|---|
| `CORTEX_ABLATE_SPREADING_ACTIVATION` | Source missing (dist-only) | No | CRIT-001 |
| `CORTEX_ABLATE_DENDRITIC_CLUSTERS` | Source missing (dist-only) | No | CRIT-001 |
| All other 21 vars | Not ported | No | Out-of-scope until Python migration |

No TypeScript code currently reads any `CORTEX_ABLATE_*` env var and produces
guarded-but-real behaviour. The ablation contract is absent in the TS layer.

---

## §6 Cross-Reference: Deferred Markers → Open ADRs / Tracking Entries

Every deferral must map to either an open ADR or an open tracking document.
Orphan deferrals are flagged.

| Marker | Deferred to | ADR / Tracking document | Status |
|---|---|---|---|
| CRIT-001/002/003 — Hopfield/HDC/SR/SA signals + source files | PG stored-procs port | None found — **ORPHAN** | Needs tracking entry |
| CRIT-004/005 — wiki-synthesize / wiki-pipeline no-ops | Phase 7 Group D (pg_store_wiki) | `docs/PHASE_7_TRACKING.md §Group D` | Open |
| CRIT-006/007 — sync façades return [] | port/mcp_client_pool | None found — **ORPHAN** | Needs tracking entry or removal |
| CRIT-008 — post-tool-capture no-op | merge order #2 (remember) | `docs/PHASE_7_TRACKING.md §merge order #2` | Open — remember package exists; wiring pending |
| CRIT-009/MED-005/006 — consolidation hooks | merge order #4 (consolidation) | `docs/PHASE_7_TRACKING.md §merge order #4` | Open |
| HIGH-001–010 — wiki handlers | Phase 7 Group D | `docs/PHASE_7_TRACKING.md §Group D` | Open |
| HIGH-011 — CodebasePort tools 18–23 | Phase 6 | `docs/PHASE_3_PLAN.md §7.5` + `packages/core/src/ports/codebase.ts:6` | Open |
| MED-001/004 — entity extraction approximation | port/cortex-graph-navigation | Referenced in co-activation.ts comment | No ADR found — **ORPHAN** |
| MED-002 — rule persistence | port/cortex-automation | Referenced in rules.ts comment | No ADR found — **ORPHAN** |
| MED-003 — fractal hierarchy | port/cortex-graph-navigation | Referenced in recall-hierarchical-handler.ts | No ADR found — **ORPHAN** |
| MED-007 — domain-mapping package | port/core-types | `docs/PHASE_7_TRACKING.md §Group A` (loose) | No dedicated ADR — **ORPHAN** |
| MED-008/009 — embedding adapter not wired at composition root | Phase 7 Group A | `docs/PHASE_7_TRACKING.md §Group A` | Open — adapter built; wiring missing |
| MED-010–012 — MCP server composition roots | Phase 6 | `packages/mcp-servers/*/src/index.ts` comments cite docs/PATTERNS.md | Open; Phase 6 not started |
| TEST-001–005 — AP graph live tests | AP fixture | None found — **ORPHAN** | Needs CI fixture tracking |
| TEST-009–011 — codebase parity tests | PR #18 | Referenced in test file | Open — PR #18 status unknown |

**Orphan count: 6** deferrals with no open ADR or tracking document.
Recommended: open six tracking entries before the next Phase begins.

---

*Audit method: grep vectors 1–12 + direct inspection of special focus areas A–H.*
*No dist/ files counted as source evidence. All file:line citations are `src/` unless noted.*
