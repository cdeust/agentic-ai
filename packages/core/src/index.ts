/**
 * @agentic/core — package placeholder.
 *
 * The full type space designed in the `port/core-types` worktree
 * (commit a9c2be3) ships in a follow-up commit. This stub exists so the
 * monorepo's tsc workspace build does not fail with TS18003 on an empty
 * src/ directory.
 *
 * source: post-Phase-4 cleanup, 2026-04-26.
 */
export const __PLACEHOLDER__ = true;
