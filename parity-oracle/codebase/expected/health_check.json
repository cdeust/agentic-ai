{
  "_capture_status": "TO-BE-CAPTURED-IN-PHASE-0-DAY-1",
  "_capture_blocked_reason": "Requires compiled Rust binary from automatised-pipeline. Run 'cargo build --release' first (see RUNBOOK.md §3.1).",
  "_capture_command": "AIPRD_PIPELINE_BIN=<path/to/binary> node scripts/capture_codebase_fixture.js health_check | node scripts/strip_nondeterministic.js > parity-oracle/codebase/expected/health_check.json",
  "_schema_shape": {
    "status": "string == 'ok'",
    "version": "string (semver)"
  },
  "_critical_invariants": [
    "status == 'ok'",
    "version is a non-empty string"
  ],
  "_masked_fields": {
    "version": "<MASKED:nondeterministic>"
  }
}
