# Fixture Repo: small-python

A minimal synthetic Python project used for parity-oracle codebase indexing tests.

Contains exactly 10 Python source files across 2 modules. Designed to produce
deterministic symbol counts for assertions: 10 files, ~20 functions, ~5 classes.

Used by: `index_codebase_smallrepo.json` input fixture.
